server:
  port: 9901
spring:
  cloud:
    consul:
      host: 192.168.1.129
      port: 8500
      discovery:
        instance-id: ${spring.application.name}
  application:
    name: IntegrationServer
  profiles:
    active: native
  devtools:
    livereload:
      enabled: true
      port: 35729
    remote:
      restart:
        enabled: true
management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      enabled: true
      show-details: always
    shutdown:
      enabled: true
logging:
  level:
    root: error
    com.cn: debug